const html = learn(html);
const css = learn(css);

buildWebsiteWithSkills(html, css);

const javascript = learn(javascript);

if (successfullyLearn(javascript)) {
  const react = learn(react);
  buildWebsiteWithSkills();
}
// to be continued
